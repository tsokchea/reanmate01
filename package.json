{
  "name": "reanmate",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Bilingual (Khmer/English) study app — React client + Flask (Python) API. See backend/README.md to run the API.",
  "scripts": {
    "dev:client": "npm run dev --prefix client"
  },
  "engines": {
    "node": ">=20"
  }
}
